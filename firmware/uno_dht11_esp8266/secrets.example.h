#pragma once

// Copy this file to secrets.h. Do not commit secrets.h to source control.
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// This must be the IPv4 address of the PC running server/server.py on the
// same Wi-Fi network. Do not use localhost or 127.0.0.1.
#define SERVER_HOST "192.0.2.10"
#define SERVER_PORT 8000

// Existing controller server accepts ZONE1 or ZONE2.
#define ZONE_ID "ZONE1"
